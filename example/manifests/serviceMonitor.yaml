apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    prometheus: k8s
  name: aws-resource-exporter
spec:
  endpoints:
    - port: http
  interval: 30s
  selector:
    matchLabels:
      app: aws-resource-exporter
