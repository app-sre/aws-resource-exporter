apiVersion: v1
kind: Service
metadata:
  name: aws-resource-exporter
  labels:
    app: aws-resource-exporter
spec:
  selector:
    app: aws-resource-exporter
  ports:
  - name: http
    protocol: TCP
    port: 9115
    targetPort: 9115
